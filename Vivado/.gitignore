# Vivado projects – ignore everything inside them
gateware_*/*

# Keep project files and source directories
!gateware_*/*.xpr
!gateware_*/*.srcs/
!gateware_*/*.srcs/**

# Ignore projects with "*_test" in their names
gateware_*_test/*

# Keep shared top-level IP repository
!IP_repository/
!IP_repository/**

# Keep this file itself
!.gitignore
